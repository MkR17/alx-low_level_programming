#include "main.h"

/**
 * _abs -a function thst computes the absolute value of an integer
 * @n: the int to be checked
 * Return: the absolute value of integery
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
